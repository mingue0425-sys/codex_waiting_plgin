# v0.7 Native Handoff E2E

THREAD_NATIVE_TERMINAL = `FAIL`

The required E2E chain is normal command, raw command integrity, provenance, native yield, 10-second boundary, turn end, heartbeat survival, zero model wait-family calls, correlated exit code, same-thread continuation, RESULT_TOKEN, and run_count=1.

Cases:

- `E2E-NATIVE-01-exit-0`: `UNKNOWN` — requires normal command, provenance, native yield, survival, completion/exit-code mapping and continuation
- `E2E-NATIVE-02-exit-7`: `NOT_RUN` — gated until E2E-NATIVE-01 native ownership is proven
- `E2E-NATIVE-03-threshold-race`: `NOT_RUN` — gated until native yield is proven
- `E2E-NATIVE-04-interrupt-race-100`: `NOT_RUN` — gated until native yield is proven
- `E2E-NATIVE-05-controller-crash`: `NOT_RUN` — gated until durable native ownership is proven
- `E2E-NATIVE-06-app-server-crash`: `NOT_RUN` — gated until native background ownership is proven
