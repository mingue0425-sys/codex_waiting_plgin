# v0.3 token/model request benchmark

Status: **UNKNOWN**

The benchmark runs the same benign two-second fixture through normal `codex exec` and a Snooze-owned App Server thread. Turn/request/tool counts come from protocol events where available. Token telemetry is reported only when the runtime exposes it; no token savings are inferred from wall time.
