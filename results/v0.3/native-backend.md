# v0.3 native App Server backend

Status: **PASS**

The experimental `process/spawn` API was exercised with a Python fixture. This proves the API's process lifecycle for the fixture only; sandbox and approval parity remain separate gates.
